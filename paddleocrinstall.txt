
1) First ensure that python and pip are installed and set in the environment path variables

2) Enter the command in cmd : pip install "paddleocr>=2.0.1"

3) Enter the command in cmd : pip install paddlepaddle

4) To check if paddleocr is working in windows open a empty python file

5) In python file add : from paddleocr import PaddleOCR

6) Run the file, it should not give any errors if paddleocr is correctly installed
